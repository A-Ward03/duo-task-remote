pipeline {
    agent any
    stages {
        stage('this is a test') {
            steps {
                sh '''
                echo "Hello from Jenkins."
                '''
            }
        }
    }
}